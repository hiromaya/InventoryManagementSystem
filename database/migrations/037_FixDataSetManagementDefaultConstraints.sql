-- =============================================================================
-- 037_FixDataSetManagementDefaultConstraints.sql
-- 
-- このスクリプトは既に完了している処理のプレースホルダーです。
-- UpdatedAtのデフォルト制約は既に存在しているため、追加の処理は不要です。
-- =============================================================================

USE InventoryManagementDB;
GO

PRINT '========================================';
PRINT 'UpdatedAtデフォルト制約 - 確認済み';
PRINT '========================================';
PRINT '';
PRINT '✅ UpdatedAtカラムのデフォルト制約は既に存在します';
PRINT '✅ 追加の処理は不要です';
PRINT '';
PRINT '処理完了';
GO